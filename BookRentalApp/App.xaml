<Application x:Class="BookRentalApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BookRentalApp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- 🎨 Base Colors -->
        <Color x:Key="PrimaryColor">#0A0A0A</Color>
        <Color x:Key="PrimaryLight">#1A1A1A</Color>
        <Color x:Key="AccentColor">#00E5FF</Color>
        <Color x:Key="AccentHover">#33F1FF</Color>

        <!-- 📝 Text -->
        <SolidColorBrush x:Key="TextBrush" Color="White" />
        <SolidColorBrush x:Key="AccentBrush" Color="#00E5FF" />

        <!-- 🔵 Neon Button Style -->
        <Style x:Key="NeonButton" TargetType="Button">
            <Setter Property="Background" Value="#111111"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#00E5FF"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>

            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#00E5FF"
                                  BlurRadius="12"
                                  ShadowDepth="0"
                                  Opacity="0.7"/>
                </Setter.Value>
            </Setter>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="2"
                            CornerRadius="6"
                            Padding="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#33F1FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 🟣 DataGrid Dark Mode -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#111"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="RowBackground" Value="#1A1A1A"/>
            <Setter Property="AlternatingRowBackground" Value="#222"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderBrush" Value="#00E5FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#333"/>
            <Setter Property="VerticalGridLinesBrush" Value="#333"/>
        </Style>

    </Application.Resources>
</Application>
