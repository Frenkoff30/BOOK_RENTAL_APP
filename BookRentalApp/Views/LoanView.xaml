<UserControl x:Class="BookRentalApp.Views.LoanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0A0A0A">

    <Grid Margin="20">
        <DataGrid x:Name="LoansGrid"
                  AutoGenerateColumns="False"
                  Background="#111"
                  Foreground="White"
                  FontSize="16" SelectionChanged="LoansGrid_SelectionChanged">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Kniha" Binding="{Binding Book.Title}" Width="200"/>
                <DataGridTextColumn Header="Zákazník" Binding="{Binding Customer.FullName}" Width="200"/>
                <DataGridTextColumn Header="Datum půjčení" Binding="{Binding LoanDate}" Width="200"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
